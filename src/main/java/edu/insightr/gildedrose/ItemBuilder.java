/*package edu.insightr.gildedrose;

import static edu.insightr.gildedrose.Inventory.AGED_BRIE;

public class ItemBuilder {
    Strategy agedBrieStrategy = new AgedBrieStrategy();

    public Item buildItem(String itemType) {
        Item newItem = null;
        if (AGED_BRIE.equals(itemType)) {
            newItem = new Item(AGED_BRIE, 2, 0, agedBrieStrategy);
        }
        return newItem;
    }
}*/
